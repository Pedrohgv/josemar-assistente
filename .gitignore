# Environment variables
.env
.env.local
.env.*.local
.envrc

# Virtual environments
.venv/
venv/
env/
ENV/
dev-tools-venv/

# Workspace (persistent data)
workspace/

# Agent state submodule (managed separately)
agent-state/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
!agent-state/**/lib/
!skills-factory/**/lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Logs
*.log

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Testing
.pytest_cache/
.coverage
htmlcov/
.tmp/

# Disposable local artifacts and test sandboxes
dump_folder/

# Docker
.docker/

# Local deepwork progress tracking
.slim/deepwork/

# BEGIN oh-my-opencode-slim worktrees
.slim/worktrees/
.slim/worktrees.json
# END oh-my-opencode-slim worktrees

# Credentials directory
# Contains sensitive OAuth and API credential files
# These are mounted into the container at runtime
credentials/

# local obsidian vault for development and testing
obsidian_vault

# local josemar vault scaffold/content
josemar-vault/

# Auxiliary ML model artifacts (local only, can be very large)
aux-ml/models/*.gguf
aux-ml/models/*.bin
aux-ml/models/*.safetensors
!aux-ml/models/.gitkeep
!aux-ml/models/README.md

# Graphify dev-tool artifacts (issue #116)
# graph.json + GRAPH_REPORT.md are committed as the onboarding map.
# The rest are regenerable caches/local state and are NOT committed.
graphify-out/manifest.json
graphify-out/cache/
graphify-out/cost.json
graphify-out/.graphify_labels.json
graphify-out/.graphify_labels.json.sig
graphify-out/.graphify_root
graphify-out/.pending_changes
graphify-out/2026-*/     # graphify auto-backup of the previous graph on prune
