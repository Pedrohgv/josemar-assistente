#!/bin/sh
# Skill sibling — delegates to the canonical workspace-sync binary.
# This file exists so the Hermes skill command-dispatch path resolves
# /opt/josemar/skills/workspace-sync/workspace-sync, which is a symlink
# to /usr/local/bin/workspace-sync in the image.
exec /usr/local/bin/workspace-sync "$@"