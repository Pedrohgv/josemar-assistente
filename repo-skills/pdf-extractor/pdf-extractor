#!/bin/bash
# PDF Extractor skill wrapper for OpenClaw

# Get the directory where this script is located
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Call the Python script from the same directory
python3 "$SCRIPT_DIR/pdf_extractor.py"
